# 开发环境 Dockerfile
FROM node:22-alpine

# 设置工作目录
WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm@8.15.0

# 复制 package.json 和相关配置文件
COPY package.json ./
COPY pnpm-lock.yaml* ./

# 安装依赖（包括开发依赖）
RUN pnpm install

# 创建必要的目录
RUN mkdir -p ./src ./public

# 暴露端口
EXPOSE 3010

# 开发环境启动命令 - 使用 Vite 开发服务器进行热更新
CMD ["pnpm", "dev"]
